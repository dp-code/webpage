export class Locale {
  name: string;
  text: string;
  icon: string;

  constructor(name: string, text: string, icon: string) {
    this.name = name;
    this.text = text;
    this.icon = icon;
  }
}
